"""ai-agents — a cross-harness catalog of agents and their workflows."""

#: The single source of truth for this project's version. `pyproject.toml`
#: declares `dynamic = ["version"]` and reads this line at build time, so a
#: release bump edits exactly one place. See CONTRIBUTING.md, "Cutting a
#: release", for what a bump means when only the catalog changed.
__version__ = "0.1.0"
