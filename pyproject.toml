[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ai-agents"
# Read from src/ai_agents/__init__.py — see [tool.hatch.version] below.
# One number in one place: `ai-agents --version` and the distribution
# metadata cannot disagree, because there is nothing to keep in sync.
dynamic = ["version"]
description = "Cross-harness catalog of agents and their workflows, with multi-level installation"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
keywords = ["agents", "workflows", "claude-code", "cline", "gemini-cli", "qwen-code"]
dependencies = ["click>=8.1"]

[project.scripts]
ai-agents = "ai_agents.cli:cli"

[dependency-groups]
dev = ["pytest>=8.0"]

# The single source of truth for the version. `__version__` in the package
# is what `ai-agents --version` prints and what the CLI has always read;
# making the build derive the distribution version from it means the bump
# in CONTRIBUTING.md's release sequence touches exactly one line.
[tool.hatch.version]
path = "src/ai_agents/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/ai_agents"]

# The agent catalog ships inside the wheel rather than being fetched at
# `init` time. `agents/` sits outside the package directory, so it has to be
# pulled in deliberately; it lands as `ai_agents/_catalog/`, which is what
# `cli._catalog_source()` reads. Reasoning is recorded in
# docs/design/2-catalog-distribution.md — briefly: `init` stays offline,
# and the catalog is version-locked to the hand-rolled frontmatter parser
# in `catalog.py` that has to read it.
[tool.hatch.build.targets.wheel.force-include]
"agents" = "ai_agents/_catalog"

[tool.hatch.build.targets.sdist]
include = [
    "src",
    "tests",
    "agents",
    "harness-adapters",
    "docs",
    "*.md",
    "pyproject.toml",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
